/**
 * Chat-specific types for the dashboard
 * Inherited from @claw/hooks for framework consistency.
 */
export * from '@claw/hooks/src/types';
import { PageContextData } from '../../types/page-context';
export type { PageContextData };
