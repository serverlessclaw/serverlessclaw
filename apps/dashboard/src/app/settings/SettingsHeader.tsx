'use client';

import React from 'react';
import PageHeader from '@/components/PageHeader';

export default function SettingsHeader() {
  return <PageHeader titleKey="SETTINGS_TITLE" subtitleKey="SETTINGS_SUBTITLE" />;
}
