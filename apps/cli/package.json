{
  "name": "@serverlessclaw/cli",
  "version": "1.0.0",
  "description": "CLI tool for managing and syncing serverlessclaw agentic stack.",
  "type": "module",
  "main": "dist/apps/cli/src/index.js",
  "bin": {
    "claw": "dist/apps/cli/src/bin.js",
    "claw-sync": "dist/apps/cli/src/bin.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/apps/cli/src/bin.js",
    "dev": "tsc -w",
    "test": "vitest run",
    "test-silent": "vitest run --reporter=dot",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "format-check": "prettier --check . --ignore-path ../../.prettierignore",
    "aiready": "aiready scan --threshold 80"
  },
  "dependencies": {
    "@serverlessclaw/core": "workspace:*",
    "@serverlessclaw/integration-github": "workspace:*",
    "commander": "^15.0.0",
    "execa": "^10.0.0",
    "fs-extra": "^11.3.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
