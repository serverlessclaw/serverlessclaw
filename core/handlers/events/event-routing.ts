/**
 * Event Routing Module
 *
 * NOTE: This file is intentionally empty.
 *
 * All event routing is now centralized in `core/lib/event-routing.ts` (DEFAULT_EVENT_ROUTING).
 * This file exists as a potential future location for handler-specific routing logic,
 * but is currently unused to avoid duplicate routing configuration.
 *
 * Import DEFAULT_EVENT_ROUTING from '../lib/event-routing' for routing configuration.
 */
