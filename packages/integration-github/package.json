{
  "name": "@serverlessclaw/integration-github",
  "version": "1.0.0",
  "description": "GitHub Sensor and Sync Integration for ServerlessClaw",
  "private": true,
  "type": "module",
  "main": "./src/index.js",
  "scripts": {
    "test": "vitest run",
    "test-silent": "vitest run --reporter=dot",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "format-check": "prettier --check . --ignore-path ../../.prettierignore",
    "aiready": "aiready scan --threshold 80"
  },
  "dependencies": {}
}
